services:
  bot:
    build: .
    restart: unless-stopped
    env_file: .env
    volumes:
      - bot-data:/data
    # No inbound ports: the bot uses long polling (outbound only).

volumes:
  bot-data:
